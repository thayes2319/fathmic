ANTHROPIC_API_KEY=
LLM_MODEL=claude-sonnet-5
UNSPLASH_ACCESS_KEY=
PORT=8788
RESEND_API_KEY=
BLUEPRINT_NOTIFY_EMAIL=thomas@gohw.net
GEMINI_API_KEY=
# Leave unset for local dev (defaults to server/data). In production this
# should point at the mount path of Render's persistent disk, e.g. /data.
DATA_DIR=
# Shared secret for GET /api/costs?key=... -- pick your own value. Leave
# unset to keep that route returning 404 (its default/safe state).
ADMIN_KEY=
